# Test case thủ công

Lưu test case theo từng module: `AUTH`, `CATALOG`, `CART`, `ORDER`, `PAYMENT`, `REVIEW`, `ADMIN_RBAC` và `DATABASE`.

Mỗi test case cần có: ID, priority, tiền điều kiện, dữ liệu test, bước thực hiện, kết quả mong đợi, kết quả thực tế, trạng thái và bug ID.

